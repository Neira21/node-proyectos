<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
    <link rel="stylesheet" href="css/edit.css">
    <style>
      /* signup.css */
    *{
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      width: 100%;
    }

    h2 {
      margin-top: 0;
      text-align: center;
    }

    form {
      width: 90%;
      margin: 0 auto;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
    }
    form div{
      margin : 15px 5px;
    }

    input[type="text"],
    input[type="password"] {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    select {
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }
    select option{
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 5px;
    }

    button {
      padding: 10px 20px;
      background-color: #28a745;
      color: #fff;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }

    button:hover {
      background-color: #218838;
    }

    p {
      margin-top: 20px;
      text-align: center;
    }
    </style>
</head>
<body>
    <h2>Editar</h2>
    
    <!-- variable stateError y message por ejs -->
    <p style="color: red;">
      <% if (statusError) { %>
        <span><%= message %></span>
      <% } %>        
    </p>

    <% if(user) { %>
      <form action="/edit" method="POST" >
        <input type="text" id="id" name="id" value="<%= user.id %>" hidden><br>
        <div>
          <label for="username">Usuario:</label><br>
          <input type="text" id="username" name="usuario" value="<%= user.usuario %>"><br>
        </div>
        <div>
          <label for="password">Contraseña:</label><br>
          <input type="password" id="password" name="password" value="<%= user.password %>" ><br>
        </div>
        <div>
          <label>Selecciona el rol</label>
          <select name="rol_id" >
              <option <%= user.rol_id==1 ? "selected" : '' %> value="1">Administrador</option>
              <option <%= user.rol_id==2 ? "selected" : '' %> value="2">Usuario</option>
              <option <%= user.rol_id==3 ? "selected" : '' %> value="3">data-entry</option>
              <option <%= user.rol_id==4 ? "selected" : '' %> value="4">data-viewer</option>
          </select>
        </div>
          <button type="submit">Guardar</button>
      </form>

    <% } %>

    <p>Ya tienes una cuenta? <a href="/login">Iniciar Sesión</a></p>
    

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    
</body>
</html>
